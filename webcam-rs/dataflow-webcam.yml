nodes:
  - id: webcam
    custom:
      source: target/debug/webcam
      inputs:
        tick: dora/timer/millis/100
      outputs:
        - image
  - id: plot
    operator:
      python: plot.py
      inputs:
        image: webcam/image
